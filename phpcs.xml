<?xml version="1.0"?>
<ruleset name="wifinow">
    <description>PHPCS</description>

    <config name="installed_paths" value="/home/mklasen/server-management/sniffers/wpcs" />

    <config name="ignore_warnings_on_exit" value="1" />

    <rule ref="WordPress"/>

    <file>.</file>
    <exclude-pattern>*/app/www/vendor/*</exclude-pattern>
    <exclude-pattern>*/app/frontend/node_modules/*</exclude-pattern>

    <config name="minimum_supported_wp_version" value="4.7"/>

    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="wifinow"/>
            </property>
        </properties>
    </rule>

</ruleset>
